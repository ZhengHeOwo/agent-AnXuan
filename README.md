## 运行前准备：
1.本地创建.env.local文件，就是.env.example那个位置

## 运行：
终端输入: go run ./agent/cmd/agent/main.go

## Bro有话说：
项目目标为"助手Agent"，
功能差的比较多，如常驻运行、网络搜索等常见助手能力，
所以第一版确认完成之前不写完整的README。

这个项目现在pull下来没法正常跑，分析模块的产物会直接喂给主模型来完整人格，找AI生成一份就行。

Bro说完了。